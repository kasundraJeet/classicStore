{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "nodemon app.js",
    "dev": "yarn start --turbo"
  },
  "dependencies": {
    "@repo/ui": "*",
    "nodemon": "^3.1.7"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^20"
  }
}
